import 'package:flutter/material.dart';

class RegisterSalePage extends StatelessWidget {
  const RegisterSalePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Registrar Venta'));
  }
}
